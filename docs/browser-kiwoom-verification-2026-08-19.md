# 브라우저 기반 키움 연결 확인 기록

## 실행 범위

공개 대시보드 `https://kiwoomdash-nrgntfm8.manus.space/`에서 **키움 연결 확인**을 실행했다. 이 동작은 OAuth 연결 상태만 점검하며, 주문·계좌 조회·주문 전송을 수행하지 않는다.

## 관찰 결과

| 항목 | 실제 관찰값 |
|---|---|
| 실행 시각 | 2026-08-19 21:05 KST |
| 배포 서버 출발지 IP | `162.220.234.138` |
| 화면 결과 | 지정 단말 인증 대기 |
| 주문 API 호출 | 없음 |
| 기존 로컬 실제 원본 | 19종목·11,419개 ka10081 일봉·2024-02-28~2026-08-19 |

## 해석

브라우저 검증은 실제 배포 서버에서 OAuth 요청을 보내므로, 키움 지정 단말 등록에는 이 실행 시점의 **배포 서버 출발지 IP**가 필요하다. 화면은 현재 단말 인증 대기 상태를 표시했으나, 오류 코드와 등록해야 할 현재 IP, 성공 후 이어지는 읽기 전용 일봉 수집·자동 연구 절차를 더 명확히 보여주도록 보완한다.

## 배포 전환 확인

`edf51863` 체크포인트 게시 직후 공개 도메인을 다시 열었을 때는 기존 연결 확인 문구가 계속 표시됐다. 개발 미리보기에는 새 **키움 연결 · 읽기 전용 검증** 패널이 정상 렌더링됐으므로, 공개 도메인의 자산·런타임 전환 상태를 다음 검증에서 별도로 확인한다.

후속 게시 `7aac583a`가 반영된 뒤 공개 도메인에서 새 패널을 확인하고 실제 검증 버튼을 실행했다. 2026-08-19 21:14 KST의 실제 결과는 **키움 8050 지정 단말 인증 대기**였으며, 그 요청의 공개 서버 출발지 IP는 `152.55.180.85`였다. 즉 배포 인스턴스 전환에 따라 출발지 IP가 앞서 관찰한 `162.220.234.138`과 달라질 수 있으므로, 화면이 표시하는 **현재 실행 시점의 IP**를 등록값으로 사용해야 한다. 고정 IP가 보장되지 않는 한 공개 서버 OAuth의 지속 검증은 보류하고, 사용자 컴퓨터의 등록 IP 연구 노드로 실제 데이터 운영을 이어간다.

## 다음 거래일 로컬 노드 준비 상태

2026-08-19 장 마감 후 사용자 컴퓨터에서 예약 작업 세 개가 모두 `Ready` 상태임을 확인했다. `KiwoomIntradayMinuteCollector`는 2026-08-20 09:00 KST, `KiwoomAutomaticOrderExecutor`는 09:01:01 KST, `KiwoomDailyBarResearch`는 15:45:45 KST에 다음 실행이 예약됐다. 분봉 수집기와 주문 실행기의 마지막 결과는 각각 `0`이었고, 일봉 연구 예약 작업의 마지막 결과는 이전 15:45 실행의 `1`이다. 같은 일봉 연구는 20:54 KST 수동 읽기 전용 실행에서 19종목×600개 일봉 동기화·데이터셋 #30001·자동 연구 #210001까지 성공했으므로, 다음 장 마감에는 예약 작업 자체의 정상 종료를 우선 확인한다.
