# 장중 생존 조건식 상세 표시 게시 확인 기록

2026-08-19 `110b7d1a` 체크포인트에서 장중 생존 조건식에 실제 종목명, 전체 규칙 트리, ka10080 1분봉 위 모의 진입·청산 차트를 추가했다. 개발 미리보기에서는 해당 상세 패널과 실제 차트 렌더링을 확인했다.

같은 버전 쿼리(`?v=110b7d1a`)로 공개 `/intraday`를 확인했을 때, 응답은 이전 카드 문구인 `실제 주문은 생성·전송하지 않습니다.`와 코드만의 종목명(`036930 036930`)을 계속 표시했다. 즉 이 시점의 공개 도메인은 체크포인트 이전 자산을 제공하고 있으며, 새 번들 전환을 다시 확인해야 한다.

후속 재게시 `b1594abd` 뒤에도 `?v=b1594abd` 공개 `/intraday`는 동일한 이전 카드 문구와 초기 대기 상태를 반환했다. 이는 코드·개발 미리보기의 문제가 아니라 공개 도메인 런타임 또는 자산 전환이 최신 체크포인트를 반영하지 않는 배포 상태다.

직접 확인한 자산 번들 `/assets/index-tvjcFrOx.js`에는 최신 문자열 `전체 조건·차트 보기`가 포함되고 이전 문구는 없었다. 이후 새 캐시 키(`?assetcheck=b1594abd-2`)로 다시 열고 조회 완료를 기다리자, 공개 화면에서 `주성엔지니어링 036930`, `전체 조건·차트 보기`, `마감 모의 청산` 등 최신 UI가 정상 렌더링됐다. 공개 도메인 반영은 성공했으며, 초기에 보인 이전 화면은 브라우저/에지 자산 캐시 전환 지연이었다.
